// 首页列表状态

let initState = {
    data:[],
    loading:true
};

function indexList( state=initState , action ){
    
    switch ( action ) {    
        default:
            return state
            break;
    }

}

export default indexList